import "./styles.css";

// ---------------------------------------------------------------

export default function CardFeneficits({ title }) {
  return (
    <div className="card-top">
      <h2 style={{ color: "#FFF", textAlign: "center" }}>{title}</h2>
    </div>
  );
}
